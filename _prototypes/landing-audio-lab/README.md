# Landing and audio design lab

Local-only comparison page for homepage journey layouts and Works-page audio
players. It is intentionally stored under `_prototypes`, so Jekyll does not
publish it.

Serve the repository root with any static server and open:

`/_prototypes/landing-audio-lab/`

The audio transports are simulated; no audio files are loaded.
